import React from 'react';
import { NavLink } from 'react-router-dom';

const Categories = () => {
    return (
        <nav role="navigation" aria-label="News categories" className='space-y-2'>
            {/* Categories will be rendered here by parent or list */}
        </nav>
    );
};

export default Categories;